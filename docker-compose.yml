version: '3.8'

services:
  kse-engine:
    build: .
    container_name: knowledge-synthesis-engine
    ports:
      - "3000:3000"
    volumes:
      - kse-data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  kse-data:
    driver: local
